worker-threads: 4
worker-fs-threads: 2
worker-monitor-threads: 2

logger-level: debug

is-testing: true

server-name: lambda-executor
user-agent: lambda-executor 0.0.1

executor-language-type: lua

dbconnection: 'postgresql://testsuite@localhost:15433/lambda-executor_main-db'

machine-hostname: lambda.miet.ru
executor-port: 8080
